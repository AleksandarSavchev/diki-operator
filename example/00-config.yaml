# SPDX-FileCopyrightText: SAP SE or an SAP affiliate company and Gardener contributors
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: config.diki.gardener.cloud/v1alpha1
kind: DikiOperatorConfiguration

# log:
#   level: info
#   format: json

# controllers:
#   complianceScan:
#     syncPeriod: 1h
#     dikiRunner:
#       podCompletionTimeout: 10m
#       namespace: kube-system
#       kubeconfig: /path/to/target/kubeconfig # optional, path to the kubeconfig for the target cluster the diki-runner will scan. If not set, the manager's kubeconfig is used.
# server:
#   healthProbes:
#     port: 8081
#   metrics:
#     port: 8080
# leaderElection:
#   resourceName: diki-operator-leader-election
#   resourceNamespace: kube-system
#   leaderElect: true
#   leaseDuration: 15s
#   renewDeadline: 10s
#   retryPeriod: 2s
#   resourceLock: leases
